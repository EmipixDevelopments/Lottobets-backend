module.exports = function (Sequelize, Schema) {
	var module = {};
	
	//module.Admin = require('./admin')(Sequelize, Schema);
	//module.Categories = require('./categories')(Sequelize, Schema, module.Admin);
	
	
	return module;
}
